[project]
name = "py-logger"
version = "0.1.0"
description = "structlog 结构化日志、审计、trace_id"
requires-python = ">=3.12"
dependencies = [
    "structlog>=24",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["py_logger"]
