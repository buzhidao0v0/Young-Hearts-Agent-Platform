[project]
name = "py-schemas"
version = "0.1.0"
description = "Pydantic DTO 与跨服务契约"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.12",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["py_schemas"]
