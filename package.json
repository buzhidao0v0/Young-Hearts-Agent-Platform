{
  "name": "young-hearts-agent-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd apps/api-server && python -m uvicorn app.main:app --reload --port 8000\" \"cd apps/web-client && npm run dev\"",
    "dev:api": "cd apps/api-server && python -m uvicorn app.main:app --reload --port 8000",
    "dev:web": "cd apps/web-client && npm run dev",
    "install:frontend": "cd apps/web-client && npm install",
    "install:backend": "pip install -r requirements.txt",
    "install:all": "npm run install:frontend && npm run install:backend",
    "build": "cd apps/web-client && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
